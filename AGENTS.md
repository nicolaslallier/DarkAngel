# AGENTS.md

This is a pre-scaffold repository. As of the `c3a46b2 (Initial commit)` state it
contains only `README.md`, `LICENSE`, and `.gitignore` — no source code,
manifests, build/test/lint config, or CI.

- No runnable commands exist yet. Do not invent `test`/`lint`/`build` commands;
  add them (and document them here) when the tooling lands.
- The `.gitignore` is a generic Python template, so Python is the likely
  language — but this is inferred, not confirmed. Confirm before assuming a
  stack.
- When the first real config (`pyproject.toml`, `uv.lock`, CI workflows, etc.)
  appears, replace this stub with the exact developer commands and conventions.
